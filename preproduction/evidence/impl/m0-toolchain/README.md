# M0 toolchain evidence

Collected during the autonomous first-ship build (see
`preproduction/final/AUTONOMOUS_BUILD_GOAL.md`).

- `toolchain-versions.txt` — node/npm/dotnet versions on the build machine
- `smoke-results.md` — validator + test + build outcomes for the M0 commit
